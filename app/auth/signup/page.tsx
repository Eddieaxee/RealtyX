import { SignUpForm } from "@/components/onboarding/signup-form";

export const metadata = {
  title: "Join RealtyX — Premium African Real Estate Fractionalization",
  description: "Create an account to institutionalize your wealth through tier-one asset ownership.",
};

export default function SignUpPage() {
  return <SignUpForm />;
}